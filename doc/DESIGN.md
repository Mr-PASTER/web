# 📋 Дизайн-документ проекта

## 🎯 Общее описание

Это веб-сайт-портфолио на базе React (Next.js), предназначенный для демонстрации работ и приема заказов. Сайт должен быть максимально оптимизирован для быстрой загрузки и поддерживать интеграцию с внешним API.

---

## 📄 Страницы сайта

### 1. 🏠 Страница-визитка (Главная)

**Путь:** `/` или `/home`

**Описание:**
Основная страница с информацией о разработчике/дизайнере.

**Содержимое:**
- Контактная информация (email, телефон, социальные сети)
- Краткое описание о себе
- 2-3 примера лучших работ (превью с ссылками на детальные страницы)
- Призыв к действию (CTA) для перехода к заказу проекта

**Особенности:**
- Минималистичный дизайн
- Быстрая загрузка
- Адаптивная верстка

---

### 2. 📚 Страница с проектами

**Путь:** `/projects`

**Описание:**
Страница со списком всех выполненных проектов.

**Содержимое:**
- Блочный список проектов (grid layout)
- Каждая карточка проекта содержит:
  - Превью изображение
  - Название проекта
  - Краткое описание
  - Технологии/стек
  - Ссылку на детальную страницу

**Детальная страница проекта:**
- Полное описание проекта
- Скриншоты/изображения
- Использованные технологии
- Ссылка на демо (если есть)
- **Отзыв заказчика** (имя, текст отзыва, рейтинг)

**Особенности:**
- Ленивая загрузка изображений (lazy loading)
- Возможность фильтрации по технологиям
- Пагинация или бесконечная прокрутка
- Данные загружаются через API

---

### 3. 📝 Страница заказа проекта

**Путь:** `/order`

**Описание:**
Страница для создания заявки на проект или отправки контактов для связи.

**Функционал:**
Два варианта взаимодействия:

**Вариант 1: Сразу сделать заказ**
- Форма с полями для создания дизайн-документа проекта:
  - Название проекта
  - Описание задачи
  - Тип проекта (веб-сайт, мобильное приложение, дизайн и т.д.)
  - Бюджет (опционально)
  - Сроки (опционально)
  - Дополнительные требования
  - Контактные данные (имя, email, телефон)

**Вариант 2: Прислать контакты для связи**
- Упрощенная форма:
  - Имя
  - Email
  - Телефон
  - Сообщение (опционально)

**Особенности:**
- Валидация полей формы
- Отправка данных через API
- Уведомление об успешной отправке
- Обработка ошибок

---

## ⚡ Оптимизация производительности

### Приоритетные задачи:

1. **🚀 Next.js оптимизации:**
   - Server-Side Rendering (SSR) для статических страниц
   - Static Site Generation (SSG) где возможно
   - Image optimization через `next/image`
   - Code splitting автоматически через Next.js

2. **📦 Оптимизация ресурсов:**
   - Минификация CSS и JavaScript
   - Сжатие изображений (WebP формат)
   - Lazy loading для изображений и компонентов
   - Оптимизация шрифтов (subset, preload)

3. **🌐 Сетевая оптимизация:**
   - Кэширование статических ресурсов
   - CDN для статических файлов
   - Gzip/Brotli сжатие
   - HTTP/2 или HTTP/3

4. **💾 Кэширование:**
   - Кэширование API запросов
   - Service Worker для offline работы (опционально)
   - Browser caching headers

5. **📱 Производительность:**
   - Минимальный bundle size
   - Tree shaking неиспользуемого кода
   - Оптимизация критического CSS
   - Дебаунсинг для поиска/фильтров

---

## 🔌 API интеграция

### Внешнее API должно поддерживать:

#### 1. **Получение списка проектов**
```
GET /api/projects
Query params:
  - page: номер страницы
  - limit: количество на странице
  - technology: фильтр по технологии
  - search: поиск по названию

Response:
{
  projects: Project[],
  total: number,
  page: number,
  limit: number
}
```

#### 2. **Получение детальной информации о проекте**
```
GET /api/projects/:id

Response:
{
  id: string,
  title: string,
  description: string,
  images: string[],
  technologies: string[],
  demoUrl?: string,
  clientReview: {
    name: string,
    text: string,
    rating: number
  }
}
```

#### 3. **Отправка заявки на проект**
```
POST /api/orders
Body:
{
  type: 'full_order' | 'contact',
  projectName?: string,
  description?: string,
  projectType?: string,
  budget?: number,
  deadline?: string,
  requirements?: string,
  name: string,
  email: string,
  phone: string,
  message?: string
}

Response:
{
  success: boolean,
  orderId?: string,
  message: string
}
```

### Обработка ошибок:
- Retry механизм для неудачных запросов
- Graceful degradation при недоступности API
- Показ понятных сообщений об ошибках пользователю

---

## 🛠 Технический стек

### Основные технологии:
- **Next.js 16** - React фреймворк с App Router
- **React 19** - UI библиотека
- **TypeScript** - типизация
- **Tailwind CSS** - стилизация

### Рекомендуемые библиотеки:
- **React Hook Form** - управление формами
- **Zod** - валидация форм
- **SWR или React Query** - управление состоянием API запросов
- **Framer Motion** - анимации (опционально)

---

## 📁 Структура проекта

```
src/
├── app/
│   ├── layout.tsx          # Корневой layout
│   ├── page.tsx            # Главная страница (визитка)
│   ├── projects/
│   │   ├── page.tsx        # Список проектов
│   │   └── [id]/
│   │       └── page.tsx    # Детальная страница проекта
│   └── order/
│       └── page.tsx        # Страница заказа
├── components/
│   ├── ui/                 # Переиспользуемые UI компоненты
│   ├── ProjectCard.tsx     # Карточка проекта
│   ├── ContactForm.tsx     # Форма контактов
│   └── OrderForm.tsx       # Форма заказа
├── lib/
│   ├── api.ts              # API клиент
│   └── utils.ts            # Утилиты
├── types/
│   └── index.ts            # TypeScript типы
└── hooks/
    └── useProjects.ts      # Кастомные хуки
```

---

## 🎨 Дизайн принципы

1. **Минимализм** - чистый и простой интерфейс
2. **Адаптивность** - корректная работа на всех устройствах
3. **Доступность** - соблюдение WCAG стандартов
4. **Производительность** - быстрая загрузка и отзывчивость
5. **UX** - интуитивная навигация и понятные формы

---

## ✅ Чек-лист реализации

### Фаза 1: Базовая структура
- [ ] Настройка роутинга для всех страниц
- [ ] Создание базовых компонентов
- [ ] Настройка API клиента

### Фаза 2: Страница-визитка
- [ ] Верстка главной страницы
- [ ] Компонент контактов
- [ ] Превью проектов

### Фаза 3: Страница проектов
- [ ] Список проектов с API
- [ ] Детальная страница проекта
- [ ] Отображение отзывов
- [ ] Фильтрация и поиск

### Фаза 4: Страница заказа
- [ ] Форма полного заказа
- [ ] Форма контактов
- [ ] Валидация
- [ ] Интеграция с API

### Фаза 5: Оптимизация
- [ ] Оптимизация изображений
- [ ] Code splitting
- [ ] Кэширование
- [ ] Тестирование производительности

---

## 📊 Метрики успеха

- **Lighthouse Score:** > 90 по всем категориям
- **First Contentful Paint (FCP):** < 1.5s
- **Time to Interactive (TTI):** < 3s
- **Bundle Size:** < 200KB (gzipped)
- **API Response Time:** < 500ms

---

## 🔄 Будущие улучшения

- Многоязычность (i18n)
- Темная тема
- Блог/статьи
- Админ-панель для управления проектами
- Интеграция с аналитикой (Google Analytics, Yandex.Metrica)

---

**Дата создания:** 2024  
**Версия:** 1.0

